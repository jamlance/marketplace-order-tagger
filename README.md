# marketplace-order-tagger

Deploy repo for the **order-tagger** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs order-tagger --push`.

Coolify: base_directory `/`, dockerfile `/apps/order-tagger/Dockerfile`.
